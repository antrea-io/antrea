#!/usr/bin/env bash

function check_ovs_liveness {
    # Check OVS agents status
    /usr/share/openvswitch/scripts/ovs-ctl status
    if [ $? != 0 ]; then
        exit 1
    fi
}

if [ $1 == agent ]; then
    exit 0
elif [ $1 == ovs ]; then
    check_ovs_liveness
else
    exit 1
fi
