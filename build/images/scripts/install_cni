#!/usr/bin/env bash

set -euo pipefail

# Todo: check version and continue installation only for a newer version

# Install OKN configuration file
install -m 644 /etc/okn/10-okn.conf /host/etc/cni/net.d/10-okn.conf

# Install OKN binary file
install -m 755 /opt/okn/bin/okn /host/opt/cni/bin/okn
